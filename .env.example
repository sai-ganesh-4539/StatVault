# PostgreSQL Data Warehouse
DB_HOST=localhost
DB_PORT=5432
DB_NAME=statvault_dw
DB_USER=your_db_user
DB_PASSWORD=your_db_password

# PGVector Collection
PGVECTOR_COLLECTION=statvault_embeddings

# Optional: HuggingFace token for private models (if needed later)
HF_TOKEN=